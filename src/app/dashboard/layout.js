/**
 * Dashboard Layout - Jeton Compatible
 * Uses client-side auth validation via useAuth hook
 * No server-side onboarding/payment gate
 */

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
